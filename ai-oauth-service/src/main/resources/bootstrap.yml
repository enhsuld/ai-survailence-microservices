spring:
  application:
    name: oauth-service
  cloud:
    config:
      uri: http://localhost:8088

logging:
  level:
    org.springframework.security: TRACE

redirect-uri: http://localhost/oauth/login